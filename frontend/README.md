# Todo Frontend (React + TypeScript + Vite)

1. Copy `.env` from `.env.example` and set VITE_API_URL (e.g. http://localhost:4000/api)
2. Install:
   ```
   cd frontend
   npm install
   npm run dev
   ```
